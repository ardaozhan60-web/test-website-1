{
  "name": "kolay-bilgi",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx live-server",
    "format": "npx prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
